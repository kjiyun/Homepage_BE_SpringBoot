name: Spring Boot & Gradle CI

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  ci:
    uses: kjiyun/CI-CD-pipeline-standard/.github/workflows/java-ci-standard.yml@master
    with:
      java-version: "17"
      distribution: "temurin"
      build-command: "false"
      test-command: "false"
      upload-artifacts: true
    secrets:
      APPLICATION: ${{ secrets.APPLICATION }}